let initialState = {
  UserId: null,
  email: 'null',
  login: 'null',
  isAuth: false,
  isFetching: false,
};

const registrationReducer = (state, action) => {
  switch (action.type) {
    case 'asd': {
      return {
        ...state,
      };
    }
  }
};

export default registrationReducer;
